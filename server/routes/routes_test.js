/**
 * @module server/lib/routes/routes_test.js
 */
'use strict';

var Code = require('code');
var Lab = require('lab');

var routes = require('./routes');

var lab = exports.lab = Lab.script();

var describe = lab.describe;
var expect = Code.expect;
var it = lab.it;

describe('server/lib/routes/routes', () => {
  it('test', (done) => {
    done();
  });
});
